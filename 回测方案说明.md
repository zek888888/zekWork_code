# 神算子正确回测方案说明

## 🎯 您提出的正确回测逻辑

```
您的本意（正确做法）：

时间点T-1: 获取过去50根15分钟K线（真实发生）
     ↓
     把这些历史数据输入给AI模型（MiniMAX/DeepSeek/Kimi）
     ↓
AI输出: 预测接下来15分钟的涨跌（up/down/flat）
     ↓
时间点T+1: 等待15分钟后，查看实际价格变化
     ↓
对比预测和实际结果，统计准确率
```

---

## ❌ 我之前方案的错误

```
我的错误做法（已废弃）：

用MACD/KDJ指标计算信号 → 模拟预测 → 对比
     ↗                           ↖
   错误！没有真正调用AI模型            错误！不是AI的真实预测
```

**问题**：
- 没有调用真实的AI API（MiniMAX/DeepSeek/Kimi）
- 只是用技术指标模拟，结果不反映AI的真实能力
- 这样的回测结果毫无意义

---

## ✅ 正确回测方案

### 方案文件
- **神算子_正确回测方案.py** - 正确的回测框架代码

### 回测流程

#### 步骤1: 获取历史数据
```sql
-- 获取2026-03-10 12:00:00之前的50根15分钟K线
SELECT datetime, open, high, low, close, volume, macd, kdj_k, kdj_d
FROM kline_data
WHERE symbol = 'BTCUSDT' 
  AND interval = '15m'
  AND datetime <= '2026-03-10 12:00:00'
ORDER BY datetime DESC
LIMIT 50
```

#### 步骤2: 构建Prompt
```
你是一位专业的加密货币交易分析师。

请基于以下BTC/USDT最近50根15分钟K线数据，预测未来15分钟的价格走势。

[历史K线数据表格...]

请分析并给出预测（JSON格式）:
{
    "prediction": "up/down/flat",
    "up_probability": 45,
    "down_probability": 55,
    "confidence": 70,
    "reasoning": "分析理由..."
}
```

#### 步骤3: 调用AI模型
- 调用 MiniMAX API
- 调用 DeepSeek-Reasoner API
- 调用 Kimi API
- 汇总三个AI的预测结果

#### 步骤4: 获取实际结果
```sql
-- 获取2026-03-10 12:00:00到12:15:00的实际价格
SELECT close FROM kline_data
WHERE symbol = 'BTCUSDT' 
  AND interval = '15m'
  AND datetime BETWEEN '2026-03-10 12:00:00' AND '2026-03-10 12:15:00'
ORDER BY datetime
```

#### 步骤5: 对比统计
- 预测 up vs 实际 up → ✅ 正确
- 预测 down vs 实际 down → ✅ 正确
- 预测 up vs 实际 down → ❌ 错误
- 统计总体准确率

---

## 📝 需要您确认的问题

### 1. 回测数据时间范围
数据库中的实际数据：
- 最新时间：2026-03-18
- 15分钟K线数量：约7372条
- 最早时间：需要查询

**问题**：数据库中是否有2026-03-10的数据用于回测？

### 2. API调用成本
- 每个测试点需要调用3个AI（MiniMAX/DeepSeek/Kimi）
- 100个测试点 × 3个AI = 300次API调用
- 估计成本：约 ¥60-100

**问题**：是否接受这个成本？

### 3. 测试点密度
- 方案A：每15分钟一个点（每天96个）
- 方案B：每30分钟一个点（每天48个）
- 方案C：每1小时一个点（每天24个）

**问题**：选择哪种密度？

---

## 🚀 下一步行动

等您确认以下问题后，我将：

1. 检查数据库中可用的回测数据时间范围
2. 接入真实的AI API（MiniMAX/DeepSeek/Kimi）
3. 运行正确回测，统计真实准确率
4. 对比不同参数（40/50/60/80/100根K线）的效果
5. 生成改进方案

---

## 📂 相关文件

| 文件 | 说明 |
|------|------|
| **回测方案说明.md** | 本文件（回测逻辑说明） |
| **神算子_正确回测方案.py** | 正确的回测代码框架 |
| **神算子_深度改进方案.md** | 之前的错误报告（仅供参考） |

---

**等待您的确认后开始执行正确回测！**
