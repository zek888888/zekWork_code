# 系统可靠性保障 - 使用指南

## 🎯 核心承诺

**除非物理关机或系统升级，否则系统永不停机**

---

## 📦 创建的文件清单

| 文件 | 作用 | 使用方法 |
|------|------|----------|
| `配置系统可靠性.sh` | 一键配置所有保障机制 | `bash 配置系统可靠性.sh` |
| `启动系统可靠性保障.sh` | 启动所有保障服务 | `bash 启动系统可靠性保障.sh` |
| `验证系统可靠性.py` | 检查系统是否配置正确 | `python3 验证系统可靠性.py` |
| `超级看门狗.py` | 监控并自动修复故障 | 自动启动 |
| `系统可靠性保障方案.md` | 设计文档 | 阅读 |

---

## 🚀 快速开始（3步）

### 第1步：配置系统（只需一次）

```bash
cd /Users/mac/.openclaw/workspace/quant-trading
bash 配置系统可靠性.sh
```

这会：
- ✅ 配置Mac永不睡眠（需要sudo密码）
- ✅ 安装系统级服务（LaunchDaemon）
- ✅ 安装用户级备用服务
- ✅ 配置开机自启动
- ✅ 启动超级看门狗

### 第2步：验证配置

```bash
python3 验证系统可靠性.py
```

预期输出：
```
✅ 系统可靠性验证通过
```

### 第3步：放心使用

现在您可以：
- 🔒 锁屏离开
- 🖥️ 关闭显示器
- 👤 注销用户

系统会持续运行！

---

## 🔍 日常监控

### 查看最近预测

```bash
# 查看最近5条预测
sqlite3 data/market_data.db "SELECT datetime(predict_initiated_at), symbol, consensus_prediction FROM ai_prediction_records ORDER BY predict_initiated_at DESC LIMIT 5;"
```

### 查看服务日志

```bash
# 系统服务日志
tail -f logs/daemon_prediction.log

# 看门狗日志
tail -f logs/super_watchdog.log

# 错误日志
tail -f logs/daemon_prediction_error.log
```

### 检查服务状态

```bash
# 查看运行中的服务
launchctl list | grep quant-trading

# 全面检查
python3 验证系统可靠性.py
```

---

## ⚠️ 重要提醒

### 必须遵守

1. **必须连接电源适配器**
   - 所有设置只在连接电源时生效
   - 使用电池时Mac仍会睡眠省电

2. **系统升级后需要检查**
   - macOS大版本升级可能重置设置
   - 升级后运行：`python3 验证系统可靠性.py`

3. **物理关机后需要重新开机**
   - 这是唯一需要您手动操作的情况
   - 开机后服务会自动启动

### 故障排查

如果系统停止运行：

1. **检查电源**
   ```bash
   pmset -g | grep sleep
   ```

2. **重启服务**
   ```bash
   bash 启动系统可靠性保障.sh
   ```

3. **重新配置**
   ```bash
   bash 配置系统可靠性.sh
   ```

4. **查看告警**
   - 检查飞书消息
   - 检查日志文件

---

## 📊 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                       Mac OS X                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  LaunchDaemon (系统级)                              │   │
│  │  • 即使用户注销也运行                               │   │
│  │  • 开机自动启动                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  LaunchAgent (用户级备用)                           │   │
│  │  • 双重保障                                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  超级看门狗                                         │   │
│  │  • 每分钟检查一次                                   │   │
│  │  • 故障自动修复                                     │   │
│  │  • 飞书告警                                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  神算子预测引擎                                     │   │
│  │  • 每15分钟运行一次                                 │   │
│  │  • 崩溃自动重启                                     │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ 验收测试

配置完成后，建议进行以下测试：

### 测试1：锁屏测试
1. 锁屏1小时
2. 解锁后运行 `python3 验证系统可靠性.py`
3. 预期：显示"✅ 系统可靠性验证通过"

### 测试2：显示器关闭测试
1. 关闭显示器（不关电脑）
2. 等待30分钟
3. 打开显示器，检查预测记录
4. 预期：预测任务持续运行

### 测试3：重启测试
1. 重启Mac
2. 等待1分钟
3. 检查服务是否自动启动
4. 预期：`launchctl list | grep quant-trading` 显示服务

---

## 📝 总结

**记住这个承诺：**

> 除非物理关机或系统升级，否则系统永不停机。

**您现在可以：**
- ✅ 安心离开电脑
- ✅ 关闭显示器
- ✅ 系统会24/7持续运行
- ✅ 故障自动恢复
- ✅ 异常情况飞书告警

**如有问题：**
1. 运行 `python3 验证系统可靠性.py`
2. 检查日志 `logs/` 目录
3. 运行 `bash 配置系统可靠性.sh` 重新配置
